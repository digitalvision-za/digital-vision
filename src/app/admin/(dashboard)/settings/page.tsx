import { Save } from "lucide-react";
import { savePrivacyAction, saveSettingsAction } from "@/app/admin/actions";
import { getAdminDashboardData } from "@/lib/admin";

export default async function AdminSettingsPage() {
  const { settings, privacyPage } = await getAdminDashboardData();
  return <div className="admin-page"><header className="admin-page-header"><div><p className="eyebrow">Global content</p><h1>Settings</h1><p>Keep contact details and public messaging accurate. Enable analytics only after your reviewed privacy notice reflects it.</p></div></header><section className="admin-panel"><h2>Public site settings</h2><form action={saveSettingsAction} className="admin-form" style={{ marginTop: "22px" }}><div className="admin-form-row"><div className="field"><label htmlFor="companyName">Company name</label><input defaultValue={settings?.company_name ?? "Digital Visions"} id="companyName" name="companyName" required /></div><div className="field"><label htmlFor="locationLabel">Location label</label><input defaultValue={settings?.location_label ?? "Cape Town, serving South Africa"} id="locationLabel" name="locationLabel" required /></div></div><div className="field"><label htmlFor="tagline">Hero tagline</label><input defaultValue={settings?.tagline ?? "Websites with a clearer point of view."} id="tagline" name="tagline" required /></div><div className="field"><label htmlFor="intro">Hero introduction</label><textarea defaultValue={settings?.intro ?? ""} id="intro" name="intro" required rows={4} /></div><div className="admin-form-row"><div className="field"><label htmlFor="contactEmail">Public email <span>Optional</span></label><input defaultValue={settings?.contact_email ?? ""} id="contactEmail" name="contactEmail" type="email" /></div><div className="field"><label htmlFor="phone">Public phone <span>Optional</span></label><input defaultValue={settings?.phone ?? ""} id="phone" name="phone" type="tel" /></div></div><label className="admin-check"><input defaultChecked={settings?.analytics_enabled} name="analyticsEnabled" type="checkbox" /> Vercel Web Analytics has been reviewed and may be enabled</label><div className="admin-form-actions"><button className="button button--primary" type="submit"><Save size={17} aria-hidden="true" /> Save settings</button></div></form></section><section className="admin-panel"><h2>Privacy page</h2><p>Publish only after legal and business review. This is content management, not legal advice.</p><form action={savePrivacyAction} className="admin-form" style={{ marginTop: "22px" }}><div className="field"><label htmlFor="privacyTitle">Title</label><input defaultValue={privacyPage?.title ?? "Privacy"} id="privacyTitle" name="privacyTitle" required /></div><div className="field"><label htmlFor="privacyContent">Reviewed privacy content</label><textarea defaultValue={privacyPage?.content ?? ""} id="privacyContent" name="privacyContent" required rows={13} /></div><label className="admin-check"><input defaultChecked={privacyPage?.is_published} name="privacyPublished" type="checkbox" /> Publish this legal page</label><div className="admin-form-actions"><button className="button button--primary" type="submit"><Save size={17} aria-hidden="true" /> Save privacy page</button></div></form></section></div>;
}